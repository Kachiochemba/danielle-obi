# Copy this file to .env for local development (cp .env.example .env)
# and fill in real values. .env is gitignored and must never be committed.

# --- Supabase (client-side, safe to expose — these ship in the browser bundle) ---
SUPABASE_PROJECT_ID="hfzgoigvczhlpmpktphk"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_KVGI1bFQaKxyMmeE-plweg_nlBf1M4p"
SUPABASE_URL="https://hfzgoigvczhlpmpktphk.supabase.co"
VITE_SUPABASE_PROJECT_ID="hfzgoigvczhlpmpktphk"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_KVGI1bFQaKxyMmeE-plweg_nlBf1M4p"
VITE_SUPABASE_URL="https://hfzgoigvczhlpmpktphk.supabase.co"

# --- Supabase (server-only, SECRET — required for admin/check-in/RSVP server functions) ---
# Get this from: Supabase Dashboard -> Project Settings -> API -> service_role key.
# NEVER commit the real value or expose it to client-side code.
SUPABASE_SERVICE_ROLE_KEY=""

# --- Email (planned, not yet wired up — see src/lib/rsvp-notify.server.ts) ---
# Required once RSVP confirmation/notification emails are implemented via the Gmail API.
# GMAIL_CLIENT_ID=""
# GMAIL_CLIENT_SECRET=""
# GMAIL_REFRESH_TOKEN=""
# ADMIN_NOTIFY_EMAILS="ochembaonyekachi.t@gmail.com"
